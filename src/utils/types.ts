export interface IAddDessert {
  name: string;
  amount: number;
  color: string;
}

export interface IDessert {
  id: string;
  name: string;
  amount: number;
  color: string;
  createdAt: string; // date-time
}
